filtered_events = render partial: "category.json.jb", locals: { category: @category }

filtered_events = @category.events

# filtered_events[:details] = {
#   event_title: @category.events.title
# }

# filtered_events[:details] = @category.events.map do |event|
#   render partial: "api/events/event.json.jb", locals: {event: @event}
# end





filtered_events